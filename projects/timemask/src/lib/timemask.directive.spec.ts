import { JpTimeMaskDirective } from './jp-time-mask.directive';

describe('JpTimeMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new JpTimeMaskDirective();
    expect(directive).toBeTruthy();
  });
});
